import css from './Logo.module.css';

        const Logo = () => {
        return (
                <div className={css.logo}>
               <h2 className={css.titleLogo}>AQUATRACK</h2>
                </div>
                );
        };

export default Logo;
        